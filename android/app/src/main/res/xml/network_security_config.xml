<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is off by default: the app talks to exactly one server, over TLS.
  On a network that is already interfering with your traffic, plaintext is the
  last thing you want.

  The single exception is loopback, so the app can be pointed at a server
  running on the same machine during development. A LAN address typed into the
  setup screen still has to be https.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
